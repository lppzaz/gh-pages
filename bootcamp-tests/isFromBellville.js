var isFromBellville = function(id){
  // created variable with a parameter.
  return id.startsWith('CY');
  //used the built in method for java 'startsWith' to identify registrations with CY
  if (id === 'CY'){
  return 'true'
  }
  else{
    return false
  }
  return id
}
